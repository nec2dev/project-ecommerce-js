class Product {
  constructor(pid, name, descripction, image, stock, price, category, weight , measure, score) {
    this.pid = parseInt(pid); // Usar 
    this.name = name; // Usar o usar marca y/o modelo
    this.descripction = descripction; // Usar 
    this.image = image; // Usar 
    this.stock = parseInt(stock); // Usar 
    this.price = parseFloat(price); // Usar 
    this.category = category;
    this.weight = parseFloat(weight);
    this.measure = measure;
    this.score = score;
  }
}


